apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: orders-secret
  namespace: ticketing
spec:
  template:
    data: null
    metadata:
      name: orders-secret
      namespace: ticketing
