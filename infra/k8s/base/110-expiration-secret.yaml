apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: expiration-secret
  namespace: ticketing
spec:
  template:
    data: null
    metadata:
      name: expiration-secret
      namespace: ticketing
