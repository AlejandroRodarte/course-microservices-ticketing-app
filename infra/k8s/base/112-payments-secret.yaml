apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: payments-secret
  namespace: ticketing
spec:
  template:
    data: null
    metadata:
      name: payments-secret
      namespace: ticketing
