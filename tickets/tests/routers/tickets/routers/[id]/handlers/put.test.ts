describe('Tests for the PUT /tickets/:id endpoint.', () => {
  describe('Success cases', () => {
    it('Should have a route handler listening on /tickets for PUT requests.', async () => {});
  });
});
